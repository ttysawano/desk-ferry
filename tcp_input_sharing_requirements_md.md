# TCP/IP 入力共有アプリ ソフトウェア要件定義書 ver.2

## 1. 目的

本ソフトウェアは、同一ネットワーク内にある複数のホストマシン間で、Windowsホストに接続された物理マウスおよび物理キーボードを共有し、Linux X11クライアントをあたかもWindowsホスト側の拡張ディスプレイのように操作できるようにすることを目的とする。

典型的な利用例は以下である。

- `host1` は Windows 10/11 マシンである。
- `host1` には物理マウス、物理キーボード、および複数の物理ディスプレイが接続されている。
- `host2` は Linux X11 マシンである。
- `host2` には単一の物理ディスプレイが接続されている。
- `host1` のあるディスプレイの右隣に、`host2` のディスプレイが物理的に配置されている。
- `host1` 側の指定された画面端からさらに外側へマウスを移動すると、操作対象が `host2` に切り替わる。
- `host2` の指定された画面端からさらに外側へマウスを移動すると、操作対象が `host1` に戻る。
- マウスカーソルが `host2` 上にある間は、Windowsホストのキーボード入力も `host2` に送られる。

本ソフトウェアは、既存のリモートデスクトップや画面転送ではなく、各ホストに接続された物理ディスプレイをそのまま利用し、マウス・キーボード入力だけをネットワーク越しに共有する。

## 2. 最終サポート構成

### 2.1 サポート対象OS

本ソフトウェアのサポート対象は、以下の構成に限定する。

```text
Server / Host:
  Windows 10/11
  複数ディスプレイ対応

Client:
  Linux on X11
  単一ディスプレイのみ対応
```

以下の構成は、初期実装だけでなく最終版においてもサポート対象外とする。

- Linuxホスト + Windowsクライアント
- Windowsクライアント
- macOSホスト
- macOSクライアント
- Waylandクライアント
- Linuxホスト
- クライアント側の複数ディスプレイ

### 2.2 ホスト・クライアント構成

本ソフトウェアは、以下の構成で動作する。

- サーバーアプリケーション: Windowsホスト上で1つ起動する。
- クライアントアプリケーション: Linux X11クライアント上で1つ以上起動する。
- 想定クライアント数: 最大3台程度。
- 初期PoCでは、Windowsホスト1台 + Linux X11クライアント1台に限定する。
- 通信方式: TCP/IP。
- 想定ネットワーク: 同一LAN内、または信頼できる閉域ネットワーク内。

### 2.3 ディスプレイ構成

#### Windowsホスト側

Windowsホスト側は、複数ディスプレイを持つことを前提とする。

Windows上の複数ディスプレイは、Windowsの仮想スクリーン座標系として扱う。

例:

```text
+-------------------+-------------------+
| host1 display A   | host1 display B   |
| 1920 x 1080       | 2560 x 1440       |
+-------------------+-------------------+
                                      +-------------------+
                                      | host2 display     |
                                      | 1920 x 1080       |
                                      +-------------------+
```

この場合、`host2` は Windows の仮想スクリーンに含まれないが、設定ファイル上では `host1` の特定ディスプレイの特定エッジに接続された外部ホストとして扱う。

#### Linux X11クライアント側

Linux X11クライアント側は、単一ディスプレイのみをサポートする。

クライアント側の複数ディスプレイ、Xinerama、複数X screen、Waylandセッションはサポート対象外とする。

## 3. 用語定義

| 用語 | 意味 |
|---|---|
| サーバー | Windowsホスト上で動作する入力共有サーバー |
| ホスト | 物理マウス・物理キーボードを持つWindowsマシン |
| クライアント | 入力イベントを受信して自ホストへ注入するLinux X11マシン |
| アクティブホスト | 現在マウスカーソルとキーボード入力の対象になっているホスト |
| 画面端 | ディスプレイまたは仮想スクリーン上の上・下・左・右の境界 |
| 境界遷移 | 画面端を越えて別ホストへ操作対象を切り替えること |
| 入力イベント | マウス移動、クリック、ホイール、キー押下、キー解放など |
| 入力注入 | 受信した入力イベントをOSの入力イベントとして発生させること |
| 入力抑止 | 物理入力をローカルOSやローカルアプリケーションに渡さないこと |
| Windows仮想スクリーン | Windowsが複数ディスプレイを統合して扱う座標空間 |

## 4. 基本動作

### 4.1 アクティブホストの概念

本ソフトウェアは、常に1つのアクティブホストを持つ。

```text
active_host = host1 | host2 | host3 | ...
```

- アクティブホストがWindowsホスト自身の場合、物理マウス・物理キーボード入力は通常どおりWindowsで処理される。
- アクティブホストがLinux X11クライアントの場合、Windowsホストは物理マウス・物理キーボード入力を取得し、原則としてWindows側へ渡さず、暗号化されたTCP通信でクライアントへ送信する。

### 4.2 WindowsホストからLinuxクライアントへの遷移

Windowsホスト上で、マウスカーソルが設定された画面端に到達し、さらに画面外方向へのマウス移動が検出された場合、サーバーは設定ファイルに従ってアクティブホストをLinuxクライアントへ切り替える。

例:

```text
host1 の display B の右端  ->  host2 の左端
```

この場合、Windowsホストの `display B` の右端からさらに右へ移動した時点で、操作対象が `host2` に切り替わる。

### 4.3 LinuxクライアントからWindowsホストへの遷移

Linux X11クライアント上で、マウスカーソルが設定された画面端に到達し、さらに画面外方向へのマウス移動が検出された場合、クライアントは自分の設定ファイルを参照し、サーバーへ境界遷移要求を送信する。

サーバーはその要求を検証し、許可された遷移であれば、アクティブホストをWindowsホストへ戻す。

### 4.4 キーボード入力

キーボード入力は、常にアクティブホストへ送信される。

- アクティブホストがWindowsホストの場合、キーボード入力はWindowsで処理される。
- アクティブホストがLinux X11クライアントの場合、Windowsホストはキーボードイベントを取得し、Linuxクライアントへ送信する。
- Linuxクライアントは、受信したキーボードイベントをX11入力イベントとして注入する。

### 4.5 クライアント側物理入力との競合

Linux X11クライアントに接続された物理マウスおよび物理キーボードは、排他的に制御しない。

クライアント側の物理入力と、Windowsホストから注入された入力が同時に発生した場合は、Linux側OSおよびX11の標準的な入力処理に従って競合してよい。

本ソフトウェアは、クライアント側の物理入力を横取り、無効化、または優先制御しない。

## 5. 入力取得・入力抑止の方針

### 5.1 Windowsホスト側の入力取得

Windowsホスト側では、物理マウスおよび物理キーボードの入力をグローバルに取得する。

初期実装では、以下のWindows APIの利用を想定する。

- Low Level Mouse Hook
- Low Level Keyboard Hook
- 必要に応じて Raw Input

### 5.2 Windowsホスト側の入力抑止

アクティブホストがWindowsホスト自身である場合、取得した入力は通常どおりWindowsへ渡す。

アクティブホストがLinuxクライアントである場合、Windowsホストは、取得した入力を原則としてWindows側のアプリケーションへ渡さず、Linuxクライアントへ送信する。

ただし、PoC段階では入力抑止が完全でない場合を許容する。

正式版では、クライアント操作中にWindows側のアプリケーションへ意図せず文字入力やクリックが行われないことを目標とする。

### 5.3 非常停止キー

非常停止キーは、アクティブホストに関わらず常にWindowsホストで処理する。

推奨する非常停止キーは以下である。

```text
Ctrl + Alt + Shift + Esc
```

非常停止キーが入力された場合、サーバーは以下を行う。

- アクティブホストをWindowsホスト自身へ戻す。
- すべての押下中キーを解放扱いにする。
- 必要に応じて、クライアントに全キー解放イベントを送る。
- 必要に応じて、クライアントに全マウスボタン解放イベントを送る。
- 入力横取り状態を解除する。

## 6. 設定ファイル要件

### 6.1 基本方針

各ホストは、自ホストにおける画面端の接続先を設定ファイルで管理する。

- Windowsホストでは、Windows仮想スクリーン全体、または特定ディスプレイの画面端に対して、遷移先を設定する。
- Linux X11クライアントでは、単一ディスプレイの上・下・左・右の画面端に対して、遷移先を設定する。

設定ファイル形式はTOMLとする。

### 6.2 Windowsホスト設定ファイル例

```toml
# server-windows.toml

[host]
name = "host1"
role = "server"
os = "windows"

[network]
bind_address = "0.0.0.0"
port = 24800

[security]
mode = "tls"
psk_file = "./secret.psk"
allow_plaintext = false

[transition]
entry_margin_px = 2
cooldown_ms = 150
require_double_push = false
edge_activation_delay_ms = 0

[input]
emergency_hotkey = "Ctrl+Alt+Shift+Esc"
keyboard_mode = "physical_key"
ime_sync = false

[windows]
# Windowsの仮想スクリーン座標を使用する。
# 起動時に実際のディスプレイ構成を自動取得する。
use_virtual_screen = true

# display id は起動時に列挙されるWindowsディスプレイ識別子または設定上の別名。
# まずはログ出力された display name を設定ファイルに転記する運用でもよい。

[[windows.displays]]
alias = "main"
match_name = "DISPLAY1"

[[windows.displays]]
alias = "right"
match_name = "DISPLAY2"

# Windows側の特定ディスプレイのエッジからLinuxクライアントへ出る設定。
# 指定形式: <target_host>:<target_edge>

[neighbors]
# Windows仮想スクリーン全体の端に対する設定。
left = ""
right = ""
up = ""
down = ""

[display_neighbors.main]
left = ""
right = ""
up = ""
down = ""

[display_neighbors.right]
left = ""
right = "host2:left"
up = ""
down = ""

[clients.host2]
name = "host2"
allowed = true
expected_fingerprint = ""
```

### 6.3 Linux X11クライアント設定ファイル例

```toml
# client-linux-x11-host2.toml

[host]
name = "host2"
role = "client"
os = "linux-x11"

[server]
host = "192.168.1.10"
port = 24800

[security]
mode = "tls"
psk_file = "./secret.psk"
allow_plaintext = false

[display]
# クライアント側は単一ディスプレイのみ対応。
# width/height は起動時にX11から自動取得する。
allow_multi_display = false

[transition]
entry_margin_px = 2
cooldown_ms = 150
require_double_push = false
edge_activation_delay_ms = 0

[input]
keyboard_mode = "physical_key"
ime_sync = false

[neighbors]
left = "host1:right"
right = ""
up = ""
down = ""

[network]
reconnect_interval_ms = 3000
```

### 6.4 neighbor指定

`neighbors` および `display_neighbors` では、各画面端を越えたときの遷移先を指定する。

指定形式は以下とする。

```text
<target_host>:<target_edge>
```

例:

```toml
right = "host2:left"
```

これは、自ホストの右端を越えたとき、`host2` の左端へ移動することを意味する。

空文字列の場合、その方向への遷移は存在しない。

```toml
up = ""
```

### 6.5 Windows側マルチディスプレイ設定の扱い

Windowsホスト側では、以下の2段階で遷移設定を扱う。

1. `display_neighbors.<display_alias>` が指定されている場合、特定ディスプレイのエッジ設定を優先する。
2. 特定ディスプレイ設定がない場合、`neighbors` によるWindows仮想スクリーン全体のエッジ設定を参照する。

初期PoCでは、特定ディスプレイの特定エッジからクライアントへ遷移する設定のみを必須とする。

## 7. 境界遷移要件

### 7.1 遷移判定

境界遷移は、以下の条件をすべて満たしたときに発生する。

- マウスカーソルが画面端に到達している。
- さらに画面外方向へのマウス移動量が検出された。
- 該当する方向に `neighbors` または `display_neighbors` の設定が存在する。
- 遷移先ホストが許可されている。
- 遷移先ホストが接続済みである。
- クールダウン期間中でない。

### 7.2 Windowsホスト側の境界判定

Windowsホスト側では、複数ディスプレイを含む仮想スクリーン座標系を取得する。

各ディスプレイについて、以下を管理する。

- ディスプレイ別名
- Windows上のディスプレイ識別子
- 左上座標
- 幅
- 高さ
- 右端座標
- 下端座標

特定ディスプレイの境界遷移は、そのディスプレイ矩形の端に対して判定する。

Windows仮想スクリーン全体の境界遷移を使う場合は、すべてのディスプレイを含む外接矩形の端に対して判定する。

### 7.3 Linux X11クライアント側の境界判定

Linux X11クライアント側では、単一ディスプレイのX11ルートウィンドウサイズを画面サイズとして扱う。

複数ディスプレイが検出された場合、初期実装ではエラーとして起動を停止するか、警告を出してメインディスプレイのみを使用する。

正式要件としては、クライアント側複数ディスプレイはサポート対象外とする。

### 7.4 座標変換

境界遷移時の位置は、遷移元エッジ上の相対位置を保存して決定する。

例として、Windowsホストのあるディスプレイ右端からLinuxクライアント左端へ移動する場合を考える。

```text
Windows側対象ディスプレイ高さ: 1440 px
遷移時のY座標: 720 px
相対位置: 720 / 1440 = 0.5

Linuxクライアント画面高さ: 1080 px
Linuxクライアント上の初期Y座標: 1080 * 0.5 = 540 px
```

このように、解像度が異なる場合でも境界上の相対位置を保存する。

### 7.5 遷移後のカーソル位置

遷移先の画面端にカーソルを完全に置くと、即時に逆方向遷移が発生する可能性がある。

そのため、遷移先では端から数ピクセル内側へ入った位置にカーソルを置く。

例:

- 左端から入る場合: `x = entry_margin_px`
- 右端から入る場合: `x = width - 1 - entry_margin_px`
- 上端から入る場合: `y = entry_margin_px`
- 下端から入る場合: `y = height - 1 - entry_margin_px`

### 7.6 遷移抑制時間

遷移直後に入力イベントが揺れて、即座に元のホストへ戻ることを避けるため、遷移後に短い抑制時間を設ける。

```toml
[transition]
cooldown_ms = 150
```

この時間中は、逆方向を含む境界遷移を無視する。

## 8. 入力イベント要件

### 8.1 マウスイベント

以下のマウスイベントを扱う。

| イベント | 必須度 |
|---|---|
| 相対移動 | 必須 |
| 境界遷移直後の絶対座標移動 | 必須 |
| 左ボタン押下・解放 | 必須 |
| 右ボタン押下・解放 | 必須 |
| 中ボタン押下・解放 | 推奨 |
| ホイール上下 | 必須 |
| 水平ホイール | 任意 |
| 追加ボタン | 初期対象外 |

通常のマウス移動は、相対移動量 `dx`, `dy` として送信する。

境界遷移直後のカーソル配置には、絶対座標指定を使用する。

### 8.2 キーボードイベント

以下のキーボードイベントを扱う。

| イベント | 必須度 |
|---|---|
| キー押下 | 必須 |
| キー解放 | 必須 |
| 修飾キー | 必須 |
| キーリピート | 推奨 |
| IME状態の同期 | 対象外 |
| キーボードレイアウト変換 | 対象外 |

修飾キーには以下を含む。

- Shift
- Ctrl
- Alt
- Meta / Super / Windows

### 8.3 キーコード方式

初期実装および正式版では、キーボード入力は物理キーコード方式で扱う。

すなわち、ネットワーク上では「文字」ではなく「物理キーの押下・解放」を送る。

内部表現としては、USB HID Usage IDに近い共通キーコードを採用する。

この方式により、以下を扱いやすくする。

- Ctrl+C などのショートカット
- Alt+Tab などのキー組み合わせ
- ファンクションキー
- カーソルキー
- 修飾キーの押下・解放

### 8.4 キーボード配列

サーバーとクライアントのキーボード配列は同一であることを前提とする。

例:

- 日本語JIS配列同士
- US配列同士

サーバーとクライアントでキーボード配列が異なる場合の文字入力の整合性は保証しない。

### 8.5 IMEの扱い

IME状態の同期は対象外とする。

日本語入力を行う場合は、アクティブホスト側のIME状態に従う。

- アクティブホストがWindowsの場合、Windows側IME状態に従う。
- アクティブホストがLinuxクライアントの場合、Linux側IME状態に従う。

Windows側IME状態とLinux側IME状態は同期しない。

## 9. 通信要件

### 9.1 通信方式

通信はTCP/IPを用いる。

サーバーは指定ポートで待ち受け、クライアントはサーバーへ接続する。

初期ポート番号は以下を仮定する。

```text
24800/tcp
```

ポート番号は設定ファイルで変更可能とする。

### 9.2 セッション

各クライアントは、サーバーに対して1本のTCP接続を確立する。

接続後、以下の初期ハンドシェイクを行う。

1. 暗号化通信の確立
2. クライアント識別子の送信
3. プロトコルバージョンの確認
4. ディスプレイ情報の交換
5. 許可されたホストであることの確認
6. 入力イベント送受信開始

### 9.3 メッセージ形式

内部プロトコルのメッセージは、初期実装ではJSON Linesを採用する。

理由は以下である。

- デバッグしやすい。
- ログにイベント種別だけを出しやすい。
- PoC段階で実装が容易である。

ただし、将来的にMessagePackまたはCBORへ移行できるよう、メッセージ生成・解析処理は抽象化する。

### 9.4 JSON Lines形式の例

#### クライアントHello

```json
{"type":"hello","host":"host2","role":"client","protocol_version":1,"display":{"width":1920,"height":1080,"multi_display":false}}
```

#### マウス移動イベント

```json
{"type":"mouse_move","dx":12,"dy":-3,"timestamp_ms":123456789}
```

#### マウス絶対座標イベント

```json
{"type":"mouse_warp","x":2,"y":540,"timestamp_ms":123456789}
```

#### マウスボタンイベント

```json
{"type":"mouse_button","button":"left","state":"down","timestamp_ms":123456790}
```

#### キーボードイベント

```json
{"type":"key","key_code":"KEY_A","state":"down","modifiers":["shift"],"timestamp_ms":123456791}
```

#### 境界遷移要求

```json
{"type":"boundary_request","from_host":"host2","from_edge":"left","to_host":"host1","to_edge":"right","pos_ratio":0.48}
```

#### アクティブホスト切替通知

```json
{"type":"active_host_changed","active_host":"host2","entry_edge":"left","entry_pos_ratio":0.48}
```

#### 全キー解放要求

```json
{"type":"release_all","reason":"emergency_stop","timestamp_ms":123456800}
```

### 9.5 プロトコルバージョン

プロトコルにはバージョン番号を持たせる。

```json
{"protocol_version":1}
```

互換性のない変更を行う場合は、メジャーバージョンを上げる。

## 10. 暗号化・認証要件

### 10.1 基本方針

本ソフトウェアはキーボード入力をネットワーク越しに送信するため、平文通信を禁止する。

同一LAN内であっても、以下の情報が通信路上で漏洩しないようにする。

- キーボードイベント
- マウスイベント
- ホスト識別情報
- 将来クリップボードを扱う場合はクリップボード内容

### 10.2 実装方式

独自暗号は実装しない。

初期実装では、標準ライブラリまたは実績のある暗号ライブラリを使い、TLSによる暗号化通信を行う。

認証には、まずは事前共有鍵ファイルを用いる。

```toml
[security]
mode = "tls"
psk_file = "./secret.psk"
allow_plaintext = false
```

実装上、TLS-PSKを直接扱うのが難しい場合は、以下のいずれかの方式を採用してよい。

1. 自己署名証明書 + 証明書フィンガープリント確認
2. TLS通信確立後にPSKベースのチャレンジレスポンス認証を行う
3. Noise Protocol FrameworkのPSKモードを使う

PoCでは、実装容易性を優先して、自己署名証明書 + フィンガープリント確認、またはTLS通信確立後のPSKチャレンジレスポンスを推奨する。

### 10.3 ローカル環境での実装容易性

ローカルLAN内でただちに実装することを考える場合、もっとも扱いやすい構成は以下である。

```text
TCP接続
  -> TLSで暗号化
  -> サーバー証明書フィンガープリントをクライアント側で確認
  -> TLS確立後、PSKを使ったチャレンジレスポンスでクライアントを認証
  -> 認証成功後に入力イベント通信を開始
```

この方式であれば、公開認証局や外部サービスは不要であり、ローカル環境だけで運用できる。

PSKファイルは、サーバーとクライアントで同一の内容を持つ。

PSKファイルは十分長いランダム値とする。

例:

```bash
openssl rand -hex 32 > secret.psk
chmod 600 secret.psk
```

Windows側では、同等にユーザー本人のみが読める権限にする。

### 10.4 なりすまし対策

サーバーは、接続してきたクライアントについて以下を確認する。

- 暗号化通信が確立されていること。
- クライアントのホスト名が許可リストに含まれていること。
- PSK認証に成功すること。
- 必要に応じて証明書フィンガープリントが一致すること。

サーバーは、クライアントが送信するホスト名だけを信用してはならない。

### 10.5 平文モード

平文TCPモードは、正式版では提供しない。

デバッグ用途で平文モードを一時的に実装する場合でも、設定ファイルで明示的に有効化した場合に限る。

```toml
[security]
allow_plaintext = false
```

標準設定では必ず `false` とする。

### 10.6 ログ出力に関する制限

キーボード入力内容をログに出力してはならない。

ログに出力してよい情報は以下に限定する。

- 接続・切断イベント
- ホスト名
- プロトコルバージョン
- 境界遷移イベント
- エラーコード
- 統計情報

以下はログに出力してはならない。

- キーコード列
- 入力された文字列
- 修飾キーを含む具体的なキーストローク列
- クリップボード内容
- PSK
- 秘密鍵

## 11. サーバーアプリケーション要件

### 11.1 起動時処理

Windowsサーバーは起動時に以下を行う。

1. 設定ファイルの読み込み
2. 設定値の検証
3. PSKまたは証明書情報の読み込み
4. Windowsディスプレイ構成の取得
5. ディスプレイ別名と実ディスプレイの対応付け
6. TCPポートの待ち受け開始
7. 暗号化通信の準備
8. ローカル入力監視の開始
9. アクティブホストをWindowsホスト自身に設定

### 11.2 Windowsディスプレイ列挙

サーバーは、起動時にWindowsのディスプレイ構成を列挙し、以下の情報をログに出力する。

- ディスプレイ識別子
- ディスプレイ名
- 座標
- 幅
- 高さ
- プライマリディスプレイかどうか

これにより、ユーザーは設定ファイル中の `match_name` や `alias` を調整できる。

### 11.3 クライアント管理

サーバーは、接続済みクライアントについて以下の情報を管理する。

- ホスト名
- 接続状態
- ディスプレイサイズ
- プロトコルバージョン
- 最終通信時刻
- 暗号化セッション状態
- 認証状態

### 11.4 入力イベント送信

アクティブホストがLinuxクライアントである場合、サーバーは以下の入力イベントをクライアントへ送信する。

- マウス相対移動
- マウスボタン押下・解放
- マウスホイール
- キー押下
- キー解放

### 11.5 接続断時の処理

アクティブホストがLinuxクライアントである状態で、そのクライアントとの接続が切れた場合、サーバーは以下を行う。

- アクティブホストをWindowsホスト自身へ戻す。
- 押下状態として記録しているキーをすべて解放扱いにする。
- 入力抑止状態を解除する。
- 警告ログを出力する。

## 12. クライアントアプリケーション要件

### 12.1 起動時処理

Linux X11クライアントは起動時に以下を行う。

1. 設定ファイルの読み込み
2. 設定値の検証
3. PSKまたは証明書フィンガープリントの読み込み
4. X11セッションであることの確認
5. 単一ディスプレイ構成であることの確認
6. サーバーへのTCP接続
7. TLS等による暗号化通信の確立
8. 認証処理
9. Helloメッセージの送信
10. ディスプレイサイズの通知
11. 入力イベント受信待ち開始

### 12.2 入力注入

クライアントは、サーバーから受信した入力イベントをX11へ注入する。

初期実装では、XTEST拡張を用いることを想定する。

対象イベントは以下である。

- マウス移動
- マウス絶対座標移動
- マウスボタン
- マウスホイール
- キー押下
- キー解放

### 12.3 境界監視

クライアントは、自ホスト上のカーソル位置を監視する。

カーソルが自ホストの画面端に達し、さらに外側方向への移動が発生した場合、クライアントは自分の設定ファイルを参照して境界遷移要求をサーバーへ送信する。

### 12.4 接続断時の動作

クライアントがサーバーとの接続を失った場合、以下の動作を行う。

- 入力注入を停止する。
- 押下状態として記録しているキーをすべて解放する。
- マウスボタン押下状態を解除する。
- 再接続を試みる。

再接続の間隔は設定可能とする。

```toml
[network]
reconnect_interval_ms = 3000
```

再接続後、サーバーは当該クライアントを非アクティブ状態として扱う。

## 13. 安全機構

### 13.1 非常停止

非常停止キーにより、いつでもアクティブホストをWindowsホスト自身へ戻せること。

非常停止後、Windows上でマウス・キーボードが通常どおり操作できること。

### 13.2 修飾キーの取り残し防止

ホスト切替時、接続断時、エラー発生時、非常停止時には、すべての修飾キーを解放状態へ戻す。

対象は以下である。

- Shift
- Ctrl
- Alt
- Meta / Super / Windows

### 13.3 マウスボタンの取り残し防止

接続断時、非常停止時、アプリケーション終了時には、押下状態として記録しているマウスボタンをすべて解放状態へ戻す。

### 13.4 誤遷移防止

以下の設定を持つ。

```toml
[transition]
entry_margin_px = 2
cooldown_ms = 150
require_double_push = false
edge_activation_delay_ms = 0
```

`require_double_push` を有効にした場合、一度画面端に当たっただけでは遷移せず、短時間内に再度外側方向へ押し込んだ場合のみ遷移する。

ただし、初期PoCでは `require_double_push = false` とする。

## 14. OS依存バックエンド要件

### 14.1 Windowsサーバーバックエンド

Windowsサーバー側には、以下のバックエンドを設ける。

```text
WindowsInputCaptureBackend
WindowsInputSuppressBackend
WindowsDisplayInfoBackend
WindowsCursorControlBackend
```

想定API:

- Low Level Keyboard Hook
- Low Level Mouse Hook
- Raw Input
- GetSystemMetrics
- EnumDisplayMonitors
- MonitorFromPoint
- GetMonitorInfo
- SetCursorPos

### 14.2 Linux X11クライアントバックエンド

Linux X11クライアント側には、以下のバックエンドを設ける。

```text
X11InputInjectBackend
X11CursorMonitorBackend
X11DisplayInfoBackend
```

想定API・拡張:

- Xlib
- XTEST extension
- XQueryPointer
- XWarpPointer

Waylandはサポート対象外とする。

## 15. 非機能要件

### 15.1 レイテンシ

ユーザーが違和感なく操作できるよう、入力イベントの遅延を小さく保つ。

目標:

- LAN内で体感遅延が目立たないこと。
- マウス移動イベントは必要に応じて合成・間引きできること。
- キー押下・解放イベントは順序を保持すること。

### 15.2 安定性

以下の状況でもアプリケーションが異常終了しないこと。

- クライアントの突然の切断
- サーバーの再起動
- ネットワーク瞬断
- 設定ファイルの不整合
- クライアントのディスプレイサイズ変更
- Windows側ディスプレイ構成の変更

### 15.3 ログ

ログは以下のレベルを持つ。

- error
- warn
- info
- debug
- trace

通常運用では `info` を使用する。

`debug` や `trace` でも、キー入力内容そのものは出力しない。

### 15.4 設定検証

起動時に設定ファイルを検証し、以下の場合は明確なエラーを出す。

- 自ホスト名が未設定
- サーバーアドレスが未設定
- ポート番号が不正
- `neighbors` の指定形式が不正
- Windows側のディスプレイ別名が実ディスプレイに対応しない
- 許可されていないホストが指定されている
- PSKファイルが読めない
- PSKファイルの権限が緩すぎる
- クライアント側がX11ではない
- クライアント側で複数ディスプレイが検出された

## 16. セキュリティ上の前提と制約

### 16.1 前提

本ソフトウェアは、信頼できる同一LAN内での利用を前提とする。

インターネット越しに直接公開してはならない。

### 16.2 禁止事項

以下を禁止する。

- 平文TCPでキーボードイベントを送信すること。
- 認証なしでクライアント接続を許可すること。
- キー入力内容をログに記録すること。
- 外部ネットワークに待ち受けポートを公開すること。
- 独自暗号を用いること。
- ホスト名だけで接続相手を信用すること。

### 16.3 推奨事項

以下を推奨する。

- ファイアウォールで接続元IPを制限する。
- サーバー待ち受けをLAN側インターフェースに限定する。
- PSKを十分長いランダム値にする。
- PSKファイルの権限を制限する。
- サーバー証明書のフィンガープリントをクライアント側で確認する。
- ログファイルの保存先をユーザーごとに分ける。

## 17. 初期実装の最小スコープ

最初のPoCでは、以下に絞る。

- Server: Windows 10/11
- Client: Linux X11
- 1 server + 1 client
- Windowsホスト側は複数ディスプレイ対応
- Linuxクライアント側は単一ディスプレイのみ対応
- 左右方向の遷移のみ
- Windows側の特定ディスプレイ右端からLinuxクライアント左端へ遷移
- Linuxクライアント左端からWindows側の特定ディスプレイ右端へ遷移
- マウス相対移動
- 境界遷移直後の絶対座標移動
- 左クリック、右クリック
- ホイール上下
- キーボード押下・解放
- 物理キーコード方式
- キーボード配列は同一前提
- IME同期なし
- クリップボード共有なし
- クライアント側物理入力との競合は許容
- 設定ファイルはTOML
- 通信は暗号化必須
- PSKまたはフィンガープリントによる認証
- 非常停止キー必須
- 手動起動のCUIアプリケーション

## 18. 初期実装で対象外とする機能

以下は初期実装の対象外とする。

- Wayland対応
- macOS対応
- Windowsクライアント対応
- Linuxホスト対応
- クライアント側複数ディスプレイ対応
- クリップボード共有
- ファイルドラッグ&ドロップ
- GUI設定ツール
- Windowsサービス化
- Linux systemd service化
- 自動ホスト検出
- mDNS
- 複雑な3台以上のレイアウト
- キーボードレイアウト変換
- IME状態同期

## 19. 受け入れ試験項目

### 19.1 基本接続

- Windowsサーバーが起動し、指定ポートで待ち受けできること。
- Linux X11クライアントがサーバーへ接続できること。
- 暗号化通信が確立されること。
- PSKまたは認証情報が一致しない場合、接続が拒否されること。
- 未許可ホスト名のクライアントが拒否されること。

### 19.2 Windows側マルチディスプレイ

- Windowsサーバーが複数ディスプレイを列挙できること。
- 各ディスプレイの座標、サイズ、識別子をログに出力できること。
- 設定ファイルで指定したディスプレイエッジから境界遷移できること。
- 指定していないディスプレイエッジでは境界遷移しないこと。

### 19.3 マウス遷移

- Windowsホストの指定ディスプレイ右端からLinuxクライアント左端へ遷移できること。
- Linuxクライアント左端からWindowsホストの指定ディスプレイ右端へ戻れること。
- 遷移後のカーソル位置が境界上の相対位置を反映していること。
- 遷移直後に意図せず元のホストへ戻らないこと。

### 19.4 キーボード入力

- アクティブホストがWindowsの場合、キーボード入力がWindowsで処理されること。
- アクティブホストがLinuxクライアントの場合、キーボード入力がLinuxで処理されること。
- Shift、Ctrl、Alt等の修飾キーが正しく扱われること。
- ホスト切替時に修飾キーが押下状態のまま残らないこと。

### 19.5 入力抑止

- アクティブホストがLinuxクライアントの場合、Windows側のアプリケーションへ意図しない文字入力が発生しないこと。
- 非常停止キーは、アクティブホストに関係なくWindows側で処理されること。

PoC段階では、入力抑止が完全でない場合も既知の制約として許容する。

### 19.6 接続断

- Linuxクライアントが切断された場合、アクティブホストがWindowsホストへ戻ること。
- 切断時にキー押下状態が解除されること。
- クライアント再起動後、再接続できること。
- 再接続後、クライアントは非アクティブ状態として扱われること。

### 19.7 非常停止

- 非常停止キーにより、いつでもアクティブホストがWindowsホストへ戻ること。
- 非常停止後にマウス・キーボードがWindows上で通常操作できること。
- クライアント側でキーやマウスボタンが押下状態のまま残らないこと。

## 20. 実装上の注意点

### 20.1 Windows入力抑止の難しさ

Windowsホスト上で物理入力を取得し、アクティブホストがLinuxクライアントのときだけローカルWindowsへ渡さない処理は、本ソフトウェアの重要な実装ポイントである。

PoCでは、まず低レベルフックでイベントを取得し、クライアント操作中はフック処理でイベントを消費する実装を試す。

必要に応じて、Raw Inputとの併用や、マウスカーソル位置の強制制御を検討する。

### 20.2 Windowsマルチディスプレイ座標

Windowsでは、ディスプレイ配置により仮想スクリーン座標が負の値を持つ場合がある。

例:

- プライマリディスプレイの左側に別ディスプレイがある場合、左側ディスプレイのX座標は負になる。
- 上側に別ディスプレイがある場合、Y座標が負になる。

そのため、境界判定は単純な `0 <= x < width` ではなく、Windows仮想スクリーン座標系に基づいて行う。

### 20.3 絶対座標と相対移動

通常のマウス移動は相対移動量で送る。

境界遷移時には、遷移先の初期位置を明示するため、絶対座標指定を使う。

### 20.4 設定不整合

各ホストが独立に設定ファイルを持つため、以下のような不整合があり得る。

- Windows側では `host2:left` を指すが、Linux側では `host1:right` を指していない。
- Linux側が存在しないホストを指している。
- サーバー側で許可されていないホストをクライアント側が指している。
- Windows側のディスプレイ別名が実ディスプレイに対応していない。

この場合、サーバーは不正な遷移要求を拒否し、ログに警告を出す。

## 21. 将来拡張項目

本要件ではサポート対象外とするが、将来別プロジェクトとして検討可能なものを以下に示す。

- クリップボード共有
- ファイルドラッグ&ドロップ
- GUI設定ツール
- ホスト自動検出
- 証明書ベースのペアリングUI
- Windowsサービス化
- Linux systemd service化
- 複雑なレイアウト編集
- MessagePackまたはCBORによるプロトコル効率化

## 22. まとめ

本ソフトウェアは、Windows 10/11ホストに接続された物理マウス・キーボードを用いて、同一LAN内のLinux X11クライアントをシームレスに操作するための入力共有アプリケーションである。

最終サポート構成は、Windowsホスト + Linux X11クライアントに限定する。

Windowsホスト側は複数ディスプレイを正式にサポートし、Linux X11クライアント側は単一ディスプレイのみをサポートする。

入力通信は暗号化を必須とし、キー入力内容をログへ出力しない。

最初のPoCでは、Windowsホストの特定ディスプレイ右端とLinuxクライアント左端を接続し、マウス移動、クリック、ホイール、キーボード押下・解放、非常停止キーを実装する。

